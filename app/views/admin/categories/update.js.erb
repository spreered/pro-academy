<% if flash[:alert].present? %>
  $(".main").prepend("<%= j(render 'shared/alert') %>")
<% else %>
  $("[data-id='<%= @category.id %>']").replaceWith("<%= j(render 'item', category: @category)%>")
<%end%>
